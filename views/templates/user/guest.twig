{% extends 'general_components/base_template.twig' %}

{% block title %}Add guest{% endblock %}

{% block body %}
    <div class="row">
        <div class="center-block" style="width: 300px;">
            <div class="login-form">
                <form action="/user/guest" method="post">
                    <h2 class="text-center">Add guest</h2>
                    <input type="hidden" name="add_guest" value="true">
                    <div class="form-group">
                        <input type="text" name="first_name" class="form-control" placeholder="First name" value="{{ guest.first_name }}" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="surname" class="form-control" placeholder="Surname" value="{{ guest.surname }}" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="father_name" class="form-control" placeholder="Father name" value="{{ guest.father_name }}" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="login" class="form-control" placeholder="Login" value="{{ guest.login }}" required>
                    </div>
                    <div class="form-group">
                        <input type="text" name="password" class="form-control" placeholder="Password" required>
                    </div>
                    <div class="form-group">
                        <input type="date" name="date" class="form-control" placeholder="By date"  value="{{ access.date_valid_from }}" required>
                    </div>
                    <select class="form-control" name="access_point" data-width="100%" required>
                        {% for key, val in points %}
                            {% if key in userPoints %}
                                <option value="{{ key }}" {% if access.access_point_id == key %} selected {% endif %}>{{ val.title }}</option>
                            {% endif %}
                        {% endfor %}
                    </select>
                    <br>
                    <div class="form-group">
                        <button id="check-form" type="button" class="btn btn-primary btn-block">Check</button>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-block">Add guest</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
{% block script %}
    $('#check-form').click(function() {
        alert( "Handler for .click() called." );
    });
    {#TODO validate form#}
    {#TODO check login unic#}
    {#TODO check date valid#}
{% endblock %}